import SignUpComponent from "@/views/sign/SignUp.vue";

const signUpRoutes = [
    {
        path: '/signup',
        name: 'signup',
        component: SignUpComponent
    }
];

export default signUpRoutes;