import LoginComponent from "@/views/login/Login.vue";

const loginRoutes = [
    {
        path: '/login',
        name: 'login',
        component: LoginComponent
    }
];

export default loginRoutes;